using System.Runtime.CompilerServices;

namespace API.Data
{
    internal class ConstConnexion
    {
        private const string user_id = "";
        private const string password = "";
        private const string source = "";

        internal const string CONNEXION = $"User Id ={user_id}; Password ={password}; Data Source = {source}";
    }
}
